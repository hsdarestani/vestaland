# Vestaland 1.2 — Native WooCommerce Client

این برنچ نسخه ۱.۲ وستالند را اضافه می‌کند. در این نسخه، فروشگاه و حساب مشتری داخل خود اپ پیاده‌سازی شده‌اند و کاربر برای محصولات، حساب، سفارش‌ها و آدرس‌ها به رابط وب سایت منتقل نمی‌شود.

## بازکردن سورس کامل

```bash
bash .source/v1.2/expand.sh
```

اسکریپت ابتدا آرشیو را از قطعه‌ها بازسازی می‌کند، SHA-256 آن را کنترل می‌کند و سپس فایل‌های پروژه را در ریشه ریپو استخراج می‌کند.

SHA-256 مورد انتظار:

```text
56298b932db5b4e83a8ee4473165c519d251ce7699f170867657056bf0ab7e39
```

## امکانات نسخه ۱.۲

- ورود داخل اپ با همان نام کاربری/ایمیل/موبایل و رمز حساب سایت
- ورود یک‌بارمصرف از طریق ایمیل یا اتصال به سرویس پیامکی وردپرس
- فهرست دسته‌ها، جست‌وجو، فیلتر، جزئیات و تنوع محصولات داخل اپ
- سبد خرید پایدار و تسویه‌حساب داخل اپ
- دریافت روش‌های ارسال و پرداخت از WooCommerce
- ایجاد سفارش واقعی WooCommerce
- نمایش پروفایل، آدرس‌ها، سفارش‌ها، وضعیت سفارش و کد رهگیری
- پرداخت در WebView امن داخل اپ، بدون بازکردن مرورگر خارجی
- عدم قرارگیری Consumer Key یا کلید مدیریتی WooCommerce در APK

## اجزای پروژه

- `app/`: اپ اندروید
- `wordpress-plugin/vestaland-bridge/`: پل امن WooCommerce و حساب مشتری
- `store-listing/`: حریم خصوصی و مستندات انتشار

## نکته نصب

پیش از تست اتصال، افزونه `vestaland-bridge` نسخه ۱.۲ باید روی سایت نصب و فعال شود. برای OTP موبایلی، فیلتر `vestaland_send_otp` باید به سرویس پیامکی فعلی سایت متصل شود.
